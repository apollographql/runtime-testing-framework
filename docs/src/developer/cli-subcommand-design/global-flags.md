# Global flags

The CLI exposes the following global flags for all subcommands to use:
- `--value`
- `--values`

## The `--value` flag
This is used to override a single value in a test plan.

## The `--values` flag
This is used to override multiple values in a test plan. This must be specified as a path to a JSON file containing the values.
