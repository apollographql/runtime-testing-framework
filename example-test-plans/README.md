# Example RTF Test Plans

Each directory here is a self contained test plan that can be validated and
executed using the `rtf` CLI tool. Depending on the test plan there may be
some required changes to values before the plan will validate.
