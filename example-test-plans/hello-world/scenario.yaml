name: hello-world-scenario
description: |
  Echo out a simple message based on the template variables provided
  in the test plan.

variable_definitions:
  - name: message
    description: "The message to echo out"
  - name: scenario_subject
    description: "The subject of the message"

command:
  name: echo-message.sh
  kind: relative_path
  path: scripts/echo-message.sh

env_vars:
  STAGE: "scenario"
  MESSAGE: "{{ message }}"
  SUBJECT: "{{ scenario_subject }}"
