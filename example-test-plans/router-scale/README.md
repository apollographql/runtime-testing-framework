# Router Scale

This is an example of how to reproduce the existing logic from [router-scale][0] under RTF.
It follows the same nested design of orchestration logic that runs locally wrapping a test
plan that is then executed on a GCP VM.


  [0]: https://github.com/apollographql/router-scale
