apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "rtf-orchestrator-ui.fullname" . }}
  labels:
    {{- include "rtf-orchestrator-ui.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "rtf-orchestrator-ui.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "rtf-orchestrator-ui.selectorLabels" . | nindent 8 }}
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: rtf-orchestrator-ui
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
              protocol: TCP
          env:
            - name: RUST_LOG
              value: {{ .Values.config.logLevel | quote }}
            - name: RTF_UI_HOST
              value: {{ .Values.config.host | quote }}
            - name: RTF_UI_PORT
              value: {{ .Values.config.port | quote }}
            - name: RTF_UI_ORCHESTRATOR_URL
              value: {{ required "config.orchestratorUrl is required" .Values.config.orchestratorUrl | quote }}
            - name: RTF_UI_GCP_PROJECT
              value: {{ required "gcpLinks.project is required" .Values.gcpLinks.project | quote }}
            - name: RTF_UI_GCP_CLUSTER_NAME
              value: {{ required "gcpLinks.clusterName is required" .Values.gcpLinks.clusterName | quote }}
            - name: RTF_UI_GRAFANA_BASE_URL
              value: {{ required "grafana.baseUrl is required" .Values.grafana.baseUrl | quote }}
            - name: RTF_UI_GRAFANA_DASHBOARD_UID
              value: {{ required "grafana.dashboardUid is required" .Values.grafana.dashboardUid | quote }}
            - name: RTF_UI_GRAFANA_DASHBOARD_SLUG
              value: {{ required "grafana.dashboardSlug is required" .Values.grafana.dashboardSlug | quote }}
            - name: RTF_UI_GRAFANA_DATASOURCE_UID
              value: {{ required "grafana.datasourceUid is required" .Values.grafana.datasourceUid | quote }}
          startupProbe:
            httpGet:
              path: /ui/health
              port: {{ .Values.service.port }}
            failureThreshold: 30
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /ui/health
              port: {{ .Values.service.port }}
            periodSeconds: 5
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
