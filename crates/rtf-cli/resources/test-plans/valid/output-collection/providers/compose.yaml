services:
  output-echo-server:
    image: python:3.14-alpine
    working_dir: /app
    volumes:
      - ${ECHO_SERVER_SCRIPT}:/app/echo-server.py:ro
    environment:
      - ECHO_MESSAGE=${ECHO_MESSAGE}
    ports:
      - "8090:8090"
    command: ["python", "/app/echo-server.py"]
