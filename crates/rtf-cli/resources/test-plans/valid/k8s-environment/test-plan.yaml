name: k8s-environment
description: Setup and teardown a kubernetes environment

variables:
  message: "hello, world!"

scenario:
  from:
    kind: local
    relative_path: scenario.yaml

environment:
  from:
    kind: local
    relative_path: environment.yaml
