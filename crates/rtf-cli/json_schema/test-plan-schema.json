{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "ConfigSpec": {
      "anyOf": [
        {
          "properties": {
            "inline": {
              "description": "Inline YAML configuration",
              "type": "object"
            }
          },
          "required": [
            "inline"
          ],
          "type": "object"
        },
        {
          "properties": {
            "from": {
              "allOf": [
                {
                  "$ref": "#/definitions/RawSource"
                }
              ],
              "description": "A source file to read to obtain the required configuration"
            },
            "overrides": {
              "default": null,
              "description": "Optional overrides to merge on top of the base configuration",
              "type": "object"
            }
          },
          "required": [
            "from"
          ],
          "type": "object"
        }
      ],
      "description": "This struct allows the config files to be resolved either from\ninline yaml in the test plan or from a [crate::providers::file::FileProvider].",
      "title": "Config Spec"
    },
    "CustomProviderDeclaration": {
      "description": "Allows for declaring one or more custom providers that should be loaded from the given\ndirectory. The directory can be specified as a relative path from the file containing\nthis declaration or as coming from a GitHub repository.\n\n## Example using a relative directory\n\n```yaml\nkind: local\nrelative_path: ../providers\nusing:\n  my_custom_provider: my_custom_provider.yaml\n  my_other_provider: my_other_provider.yaml\n```\n\n## Example using a directory from a GitHub repository\n\n```yaml\nkind: github\norg: my-org\nrepo: my-repo\npath: path/to/providers\ngit_ref: main\nusing:\n  my_custom_provider: my_custom_provider.yaml\n  my_other_provider: my_other_provider.yaml\n```",
      "oneOf": [
        {
          "properties": {
            "kind": {
              "const": "local",
              "type": "string"
            },
            "relative_path": {
              "description": "A relative path to the target config file",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "relative_path"
          ],
          "type": "object"
        },
        {
          "properties": {
            "git_ref": {
              "default": null,
              "description": "An optional git ref to pull the config file from (defaults to mainline)",
              "type": [
                "string",
                "null"
              ]
            },
            "kind": {
              "const": "github",
              "type": "string"
            },
            "org": {
              "description": "The GitHub org to pull the config file from",
              "type": "string"
            },
            "path": {
              "description": "The absolute path to the config file within the target GitHub repository",
              "type": "string"
            },
            "repo": {
              "description": "The GitHub repository to pull the config file from",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "org",
            "repo",
            "path"
          ],
          "type": "object"
        }
      ],
      "properties": {
        "using": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "A map of the name to use for a given custom provider to the child path within this\ndirectory containing the definition for that provider.",
          "type": "object"
        }
      },
      "required": [
        "using"
      ],
      "title": "Custom Provider Declaration",
      "type": "object"
    },
    "Matrix": {
      "description": "A matrix of user provided variable dimensions that is expanded out into multiple variable sets for\ntemplating the test plan containing the matrix.",
      "properties": {
        "dimensions": {
          "additionalProperties": {
            "items": {
              "$ref": "#/definitions/Scalar"
            },
            "type": "array"
          },
          "description": "The actual dimensions and their allowed variables that will be used to produce the matrix",
          "type": "object"
        },
        "include": {
          "default": [],
          "description": "Additional _groups_ of variables that will be combined with known dimensions to produce the\nfull set of variables for each dimension.\n\nUsed for tying subsets of variables together and reducing the number of variants we expand to.\nEach entry within this array is required to define the same keys and scalar variable types.",
          "items": {
            "additionalProperties": {
              "$ref": "#/definitions/Scalar"
            },
            "type": "object"
          },
          "type": "array"
        },
        "variant_names": {
          "default": null,
          "description": "Optional template string for customising the names of each variants output subdirectory.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "dimensions"
      ],
      "type": "object"
    },
    "Number": {
      "description": "Represents a number, whether integer or floating point.",
      "title": "Number",
      "type": "number"
    },
    "RawMatrix": {
      "anyOf": [
        {
          "$ref": "#/definitions/Matrix"
        },
        {
          "additionalProperties": {
            "items": {
              "$ref": "#/definitions/Scalar"
            },
            "type": "array"
          },
          "type": "object"
        }
      ],
      "description": "We support only providing dimensions at the top level if the user doesn't care about\ncustomising matrix variant names or using more advanced features.\n\nThis enum is only used to upgrade raw dimensions into a [Matrix] when parsing test plans."
    },
    "RawSource": {
      "oneOf": [
        {
          "properties": {
            "kind": {
              "const": "local",
              "type": "string"
            },
            "relative_path": {
              "description": "A relative path to the target config file",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "relative_path"
          ],
          "type": "object"
        },
        {
          "properties": {
            "git_ref": {
              "default": null,
              "description": "An optional git ref to pull the config file from (defaults to mainline)",
              "type": [
                "string",
                "null"
              ]
            },
            "kind": {
              "const": "github",
              "type": "string"
            },
            "org": {
              "description": "The GitHub org to pull the config file from",
              "type": "string"
            },
            "path": {
              "description": "The absolute path to the config file within the target GitHub repository",
              "type": "string"
            },
            "repo": {
              "description": "The GitHub repository to pull the config file from",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "org",
            "repo",
            "path"
          ],
          "type": "object"
        }
      ],
      "title": "Config Source"
    },
    "Scalar": {
      "anyOf": [
        {
          "allOf": [
            {
              "$ref": "#/definitions/Number"
            }
          ],
          "description": "Represents a number, whether integer or floating point."
        },
        {
          "description": "Represents a boolean",
          "type": "boolean"
        },
        {
          "description": "Represents a string",
          "type": "string"
        }
      ],
      "description": "A scalar that is valid to be used as a template variable for a [Field].",
      "title": "Scalar"
    }
  },
  "description": "The top level config file for specifying an RTF test plan",
  "properties": {
    "custom_providers": {
      "default": [],
      "description": "Custom provider declarations to load for this test plan",
      "items": {
        "$ref": "#/definitions/CustomProviderDeclaration"
      },
      "type": "array"
    },
    "description": {
      "description": "A brief description of the purpose / behaviour of this test plan",
      "type": "string"
    },
    "environment": {
      "allOf": [
        {
          "$ref": "#/definitions/ConfigSpec"
        }
      ],
      "description": "The environment setup and teardown to run around the test scenario"
    },
    "matrix": {
      "allOf": [
        {
          "$ref": "#/definitions/RawMatrix"
        }
      ],
      "default": {
        "dimensions": {},
        "include": [],
        "variant_names": null
      },
      "description": "Sets of templating variables to apply to fields within the rest of the test plan as a matrix"
    },
    "name": {
      "description": "The name of this test plan",
      "type": "string"
    },
    "scenario": {
      "allOf": [
        {
          "$ref": "#/definitions/ConfigSpec"
        }
      ],
      "description": "The test scenario to execute"
    },
    "variables": {
      "additionalProperties": {
        "$ref": "#/definitions/Scalar"
      },
      "default": {},
      "description": "Templating variables to apply to fields within the rest of the test plan",
      "type": "object"
    }
  },
  "required": [
    "name",
    "description",
    "scenario",
    "environment"
  ],
  "title": "Test Plan Config",
  "type": "object"
}