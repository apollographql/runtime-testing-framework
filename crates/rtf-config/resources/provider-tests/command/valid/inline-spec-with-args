A valid yaml fragment for a command section specified with
an inline spec with args

-- config.yaml --
command: 
  name: inline-command.sh
  kind: inline
  content: |
      echo "Hello, world!"
  args:
    - "arg1"
    - "{{ arg2 }}"
