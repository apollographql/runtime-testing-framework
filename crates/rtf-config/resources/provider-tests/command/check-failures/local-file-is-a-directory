A YAML fragment for a command section that references a directory
rather than a file. This should parse but fail to check successfully.

-- config.yaml --
command:
  name: inline-command.sh
  kind: inline
  content: |
    #!/usr/bin/env sh
    echo "Hello, world!"

env_vars:
  AUTH_HEADER: "{{ credentials_env_var }}"
  SERVER_URL: "{{ server_url }}"

file_providers:
  - name: "inline.txt"
    env_var: INLINE_FILE
    kind: relative_path
    path: ../../../provider-tests

-- check-errors --
IsADirectory
