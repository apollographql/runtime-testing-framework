A YAML fragment for an local path file provider that references a directory
rather than a file. This should parse but fail to validate.

-- config.yaml --
kind: local_path
relative_path: ../../provider-tests

-- validation-errors --
IsADirectory
