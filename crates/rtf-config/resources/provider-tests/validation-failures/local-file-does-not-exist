A YAML fragment for an local path file provider that references a file
that doesn't exist. This should parse but fail to validate.

-- config.yaml --
kind: local_path
relative_path: ../does-not-exist.txt

-- validation-errors --
FileNotFound
