A YAML fragment for a relative path file provider that references a file
which doesn't contain utf-8 text. This should parse and check successfully but
fail to template when we try to read it as a string.

-- config.yaml --
kind: relative_path
path: ../../../frog-no.gif

-- resolution-errors --
stream did not contain valid UTF-8
