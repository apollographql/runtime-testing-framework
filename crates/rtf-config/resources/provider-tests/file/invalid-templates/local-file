A valid yaml fragment for an local path file provider combined with
a values map that attempts to template an integer rather than a string.

-- config.yaml --
kind: local_path
relative_path: "{{ some_path }}"

-- values --
some_path: 42

-- templating-errors --
InvalidData
