A valid yaml fragment for an relative path file provider combined with
a values map that attempts to template an integer rather than a string.

-- config.yaml --
kind: relative_path
path: "{{ some_path }}"

-- values --
some_path: 42

-- templating-errors --
InvalidData
