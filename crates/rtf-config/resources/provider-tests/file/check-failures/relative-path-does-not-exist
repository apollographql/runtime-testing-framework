A YAML fragment for a relative path file provider that references a file
that doesn't exist. This should parse but fail to check successfully.

-- config.yaml --
kind: relative_path
path: ../does-not-exist.txt

-- check-errors --
FileNotFound
