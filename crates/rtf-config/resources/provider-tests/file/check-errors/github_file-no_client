A YAML fragment for a GraphOS supergraph provider. The context used in this test
does not provide config for the platform api so this test should fail checks.

-- config.yaml --
kind: github_file
org: "org"
repo: "repo"
path: "path"

-- check-errors --
MissingGithubApiKey
