A YAML fragment for a merge YAML file provider that references a file
that doesn't exist in the overrides.
This should parse but fail to check successfully.

-- config.yaml --
kind: merge_yaml
base:
  kind: inline
  content: content
overrides:
  kind: relative_path
  path: ../does-not-exist.txt

-- check-errors --
FileNotFound
