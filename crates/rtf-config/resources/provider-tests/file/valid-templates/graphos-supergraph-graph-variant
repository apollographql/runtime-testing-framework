A valid yaml fragment for a graphos supergraph file provider that uses
a template for the variant

-- config.yaml --
kind: graphos_supergraph
graph_id: "graph_id"
variant: "{{ variant }}"

-- values --
variant: "variant"

-- after-templating --
kind: graphos_supergraph
graph_id: "graph_id"
variant: "variant"
