A YAML fragment for an local path file provider that references a file
which doesn't contain utf-8 text. This should parse and validate but
fail to resolve when we try to read it as a string.

-- config.yaml --
kind: relative_path
path: ../../../frog-no.gif

-- resolution-errors --
stream did not contain valid UTF-8
