apiVersion: apps/v1
kind: Deployment
metadata:
  name: mock-backend
  namespace: orchestrator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mock-backend
  template:
    metadata:
      labels:
        app: mock-backend
    spec:
      containers:
        - name: mock-backend
          image: mock-backend
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          readinessProbe:
            tcpSocket:
              port: 8080
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: mock-backend
  namespace: orchestrator
spec:
  type: NodePort
  selector:
    app: mock-backend
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
