services:
  nginx:
    image: nginx:alpine
    labels:
      rtf.fileproviders: true
    command: >
      ls -R /providers && sleep infinity
    pull_policy: always
    volumes:
      - providers:/providers

volumes:
  providers:
    driver: local
