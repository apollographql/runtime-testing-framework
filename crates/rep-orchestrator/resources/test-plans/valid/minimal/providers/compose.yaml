services:
  nginx:
    image: nginx:alpine
    labels:
      rtf.io/file-providers: true
    command: sleep infinity
    pull_policy: always
    volumes:
      - providers:/providers

volumes:
  providers:
    driver: local
