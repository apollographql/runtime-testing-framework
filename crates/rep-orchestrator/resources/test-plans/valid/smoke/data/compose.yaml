services:
  nginx:
    labels:
      rtf.fileproviders: true
    image: nginx:alpine
    command: sleep infinity
    pull_policy: always
    volumes:
      - providers:/providers

volumes:
  providers:
    driver: local
