services:
  nginx:
    labels:
      rtf.io/file-providers: true
    image: nginx:alpine
    command: sleep infinity
    pull_policy: always
    volumes:
      - providers:/providers

volumes:
  providers:
    driver: local
