name: error-unknown-docker-image-scenario
description: Fixture for testing Unrunnable status when scenario image pull fails

scenario:
  from:
    kind: local
    relative_path: scenario.yaml

environment:
  from:
    kind: local
    relative_path: environment.yaml
