name: Test RTF Orchestrator Run Action

on: workflow_dispatch

jobs:
  run-sanity-check:
    name: Run sanity check test plan
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - uses: apollographql/release-tooling/rtf-orchestrator-run@f56ff77e945602e3ace1d159301a584391c4de13
        with:
          test_plan_path: crates/rep-orchestrator/resources/test-plans/valid/smoke/test-plan.yaml
          verbose: true
